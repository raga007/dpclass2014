#include <stdio.h>

int foo(int x) {
  int y;
  printf("y = %d\n", y);
  y = x;
  return y;
 }

int bar() {
  int A[] = {1,2,3,5};
  return 0;
}

int main() {
  int a = 0;
  a += foo(42);
  bar();
  a += foo(39);
  printf("%d\n", a);
}

